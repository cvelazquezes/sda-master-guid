/**
 * Shared Types - Single Entry Point
 *
 * All shared types should be imported from this index file.
 *
 * @example
 * import { BackgroundColor, StatusType, ComponentSize } from '@shared/types';
 */

export * from './theme';
