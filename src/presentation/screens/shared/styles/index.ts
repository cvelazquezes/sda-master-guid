/**
 * Shared Styles Index
 *
 * Central export point for all shared style patterns.
 * Import from here to use common styles across screens.
 *
 * @example
 * import { containerStyles, sectionStyles, cardStyles } from '../../shared/styles';
 */

export {
  containerStyles,
  sectionStyles,
  cardStyles,
  modalStyles,
  avatarStyles,
  statusBadgeStyles,
  listRowStyles,
  emptyStateStyles,
  filterStyles,
  footerStyles,
} from './common';

